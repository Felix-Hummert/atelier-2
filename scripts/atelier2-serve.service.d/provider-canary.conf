# A successful billed Serve start is the post-deploy trigger. --no-block keeps
# the long-lived serve activation independent of the billed canary duration.
[Service]
ExecStartPost=/usr/bin/systemctl --user --no-block start atelier2-provider-canary.service
