[project]
name = "atelier-2"
version = "0.0.0"
requires-python = "==3.12.3"
dependencies = [
    "dbos==2.29.0",
    "sqlalchemy==2.0.51",
]

[dependency-groups]
dev = [
    "pytest==9.1.1",
    "pytest-xdist==3.8.0",
]

[tool.pytest.ini_options]
addopts = "--strict-config --strict-markers"
