{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "additionalProperties": false,
  "required": ["variants", "decisions", "contradictions"],
  "properties": {
    "variants": {
      "type": "array",
      "minItems": 2,
      "maxItems": 3,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["id", "title", "sketch", "tradeoffs"],
        "properties": {
          "id": {"type": "string", "minLength": 1},
          "title": {"type": "string"},
          "sketch": {"type": "string"},
          "tradeoffs": {
            "type": "array",
            "items": {"type": "string"}
          }
        }
      }
    },
    "decisions": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["id", "question", "default_answer", "why"],
        "properties": {
          "id": {"type": "string", "minLength": 1},
          "question": {"type": "string"},
          "default_answer": {"type": "string"},
          "why": {"type": "string"}
        }
      }
    },
    "contradictions": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["owner", "text"],
        "properties": {
          "owner": {"type": "string"},
          "text": {"type": "string"}
        }
      }
    }
  }
}
