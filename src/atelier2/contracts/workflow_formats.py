"""The workflow format a run persists and a document declares.

One closed set, written once. The store CHECK, the persisted-event
constructor, and every reader that branches on the family construct or
compare this type rather than restating 1, 2, 3.
"""

from __future__ import annotations

from enum import IntEnum


class WorkflowFormatVersion(IntEnum):
    V1 = 1
    V2 = 2
    V3 = 3
