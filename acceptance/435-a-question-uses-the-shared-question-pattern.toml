schema_version = 1
story = "https://github.com/FlexOr2/atelier-2/issues/435"

# The sentence's "today's" enumeration shrank by one on 31.08.2026 (#901
# slice 4, #924): ReconciliationActionCard was deleted with the V1/V2 run
# surface, so it left the list the same way #224's clause was replaced when a
# landing made it untrue -- the identifier and the rule stand unchanged.

[[sentence]]
id = "a-question-uses-the-shared-question-pattern"
requirement = "REQ-UIQ-07"
text = "A dialog, confirmation, answer-taking sheet, or script-level confirm in a room page or a .svelte file that page imports is the shared question component or a rendering of it, not markup that file composed; today's AddModelSheet, CatalogImportSheet, ConnectSourceSheet, DisconnectSourceSheet, RenewSourceTokenSheet, WorkflowStartSheet, RunForkSheet, and RunCancelCard each compose their own dialog, which this sentence does not prove they share; a createElement overlay, a question in a .ts module, and an in-page decision stage that is not a dialog or confirm are outside what the templates and script tags show."
