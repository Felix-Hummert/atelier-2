schema_version = 1
story = "https://github.com/FlexOr2/atelier-2/issues/301"

[[sentence]]
id = "runner-child-landlock"
text = "The free child receives a kernel denial when it directly reads Runner identity material."

[[sentence]]
id = "runner-cancel-none"
text = "After STARTED, a cancelled child is reaped and its exit is observed against the signal timeline (exited before signal, reaped after TERM, or reaped after KILL)."
