schema_version = 1
story = "https://github.com/FlexOr2/atelier-2/issues/658"

# This is Hop 46's durable claim, not the chat door or the broader conversation
# experience: one run turns the same declared Wait three times, and every turn
# keeps its own attributed receipt after the store is reopened. An answer sent
# with the execution fence of an earlier turn is refused before it can write.

[[sentence]]
id = "one-run-keeps-three-attributed-turns-and-refuses-a-stale-round"
text = "One durable run accepts three execution-specific operator answers across three turns and a store reload, retains three ordered attributed WAIT_ANSWERED receipts, and refuses an answer fenced to an already answered round without writing into the current round."
