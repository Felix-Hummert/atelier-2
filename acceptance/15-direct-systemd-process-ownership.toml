schema_version = 1
story = "https://github.com/FlexOr2/atelier-2/issues/15"

# This foundation head closes only the sentence below. The five other literal
# acceptance sentences stay OPEN to the Slice2 cutover named in Issue #15:
# cancel dispositions, predecessor PREPARED/WATCHDOG_READY/LAUNCH_ARMED resume,
# RECOVERY_HANDOFF/close/finalize sharing, predecessor inode/GC witnesses, and
# the final predecessor watchdog/socket/attempt-lock residue postcondition.
# Component and real-host proofs in this head preserve their foundations; they
# are deliberately not marked as substitutes for those five complete wishes.

[[sentence]]
id = "no-global-runtime-serialization-is-introduced"
text = "No global runtime serialization is introduced."
