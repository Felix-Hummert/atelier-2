schema_version = 1
story = "https://github.com/FlexOr2/atelier-2/issues/449"

# One sentence. "existing-runtime-lines-survive-the-binder-deletion" was
# retracted by the operator on 30.08.2026 (ruling in #901: "bis das Atelier ein
# Produkt ist, brauchen wir kein Fallback und keine Backwards-Compatibility")
# and deleted with the V1 execution path it protected (#901 slice 1b, #923):
# 239 live runs carry `workflow_format_version` min = max = 3, so the sentence
# promised the preservation of a generation no operator could reach. Its one
# live half -- the V3 chain that starts, runs autonomously and reaches its
# terminal hash -- stays declared by #194's
# `a-v3-run-drives-itself-through-the-runtime`.

[[sentence]]
id = "an-authored-v3-subworkflow-remains-unexecutable-without-start-writes"
text = "V3 subworkflow and iterate declarations read back exactly as authored; executable admission and the real starter refuse them before run, configuration, or enqueue writes."
